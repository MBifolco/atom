# Original Vision Docs

These documents capture the initial architecture and product vision for Atom
Combat.

Suggested reading order:

1. `concept.md`
2. `components.md`
3. Component-specific specs (`arena_component.md`, `combat_protocol.md`, etc.)

Notes:

- `COMPONENT_OVERVIEW.md` is kept as a historical snapshot from an earlier
  architecture pass.
- For current implementation details, use `docs/README.md` and the active
  runtime/training docs under `docs/`.
