# Atom Combat - Colab-pinned training dependencies
#
# This file intentionally pins the Python-side training stack used in Colab so
# we can reproduce runs more reliably across sessions.
#
# Notes:
# - JAX is installed separately by scripts/colab/bootstrap.sh because the wheel
#   is platform-specific and already version-pinned there.
# - Torch is left to the Colab base image / regular requirements resolver to
#   avoid unnecessary large CUDA wheel churn during bootstrap.

-r requirements.txt

stable-baselines3==2.7.0
gymnasium==1.1.1
numpy==2.0.2
tensorboard==2.20.0
chex==0.1.91
onnx==1.19.1
onnxruntime==1.19.2
onnxscript==0.5.6
